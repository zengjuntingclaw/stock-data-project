# 2026-04-12

## 架构收口 Bug 修复

### P0 Bug 修复（commit: 1ca4c32）

**1. `production_data_system.py` _get_now() 递归 bug**
- 原始代码：`return _get_now()` → 无限递归
- 修复：`return datetime.now()`

**2. `data_qa_pipeline.py` NameError**
- 原始代码：使用 `Any` 类型但未导入
- 修复：`from typing import Dict, List, Optional, Tuple, Set, Callable, Any`

**3. `pipeline_data_engine.py` sync_stock 类型比较 bug**
- 原始代码：`checkpoint >= end` (date vs str)
- 修复：`str(checkpoint) >= str(end)`

### P0 兼容层补全

**4. `request_throttler.py` 补全 RequestThrottler 类**
- 原 stub 只有别名，缺少 `RequestThrottler` 类实现
- 新增完整 facade 类，含 `register_source()` / `acquire()` / `get_source_status()`

**5. `enhanced_data_engine.py` 补全 DownloadRecord.to_dict()**
- 原始：stub 无 `to_dict()` 方法
- 新增完整实现

**6. `pipeline_data_engine.py` __all__ 补全 RateLimitConfig**

### P1 测试重写

**7. `test_robust_data_pipeline.py` 全部重写**
- 50 个测试，全部匹配 PipelineDataEngine 真实 API
- 覆盖：入口导入、RuntimeController、DataRouter、QualityEngine、StorageManager、BacktestValidator、兼容层

**8. `test_enhanced_data_engine.py` 全部重写**
- 覆盖 EnhancedDownloader/EnhancedValidator/DownloadRecord/PipelineDataEngine/DataRouter 兼容测试

**9. `test_time_mock.py` 移除过时测试**
- 移除 `test_data_consistency_checker_get_now_mockable`（旧模块已合并）

**测试结果：356/356 通过 ✅**
