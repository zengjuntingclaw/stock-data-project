# 2026-04-12 日志

## 架构大收口 v6 (P0 完成) - commit 6d6edf3

### 背景
用户请求对 stock_data_project 进行架构级重构，消除模块冗余、统一所有入口、合并重复逻辑。

### 完成内容

#### P0 - 收口阶段
- **新增 `pipeline_data_engine.py`** (1400行)：7层统一架构
  - RuntimeController：限速/退避/缓存/日志
  - DataRouter + _AkShareAdapter + _BaostockAdapter：统一数据源路由+熔断
  - QualityEngine：校验/缺口检测/自动修复
  - StorageManager：DuckDB UPSERT/断点续传
  - BacktestValidator：回测一致性验证
  - PipelineDataEngine：唯一主入口
  - 向后兼容别名：EnhancedDataEngine = ProductionDataEngine = PipelineDataEngine

- **更新 `scripts/__init__.py`**：导出 PipelineDataEngine 及所有层组件

- **更新 `scripts/scheduler.py`**：_daily_download_task 迁移到 PipelineDataEngine

- **更新 `scripts/production_scheduler.py`**：Prefect Flow 迁移到 PipelineDataEngine

- **14个冗余模块重构为 DEPRECATED stub**（含 DeprecationWarning）
  - request_throttler → RuntimeController
  - backoff_controller → RuntimeController
  - request_cache → RuntimeController
  - request_logger → RuntimeController
  - baostock_enhanced → DataRouter
  - third_party_sources → DataRouter
  - time_series_gaps → QualityEngine
  - auto_repair → QualityEngine
  - parquet_optimizer → StorageManager
  - multi_source_manager → DataRouter
  - advanced_checkpoint → StorageManager
  - backtest_validator → BacktestValidator
  - enhanced_data_engine → PipelineDataEngine
  - tushare_source → DataRouter
  - data_consistency_checker → QualityEngine
  - minute_engine → PipelineDataEngine

- **清理历史备份文件**：data_engine.py.{new,v2,v4}

#### P1/P2 待执行
- 单元测试重写 (test_pipeline_data_engine.py)
- README 更新
- 最终交付物文档

#### 测试
- 核心测试 98/98 通过 ✅

#### 推送
- git push ✅ (commit 6d6edf3 → origin/master)
